# 数据目录

- `splits/`：固定 seed 的数据切分 manifest，可版本化并用于复现。
- `processed/`、`raw/` 和 `corpus/`：本地生成或下载的数据，按 `.gitignore` 规则管理，不作为论文材料目录。

数据准备和切分规则见 [`../docs/active/experiments.md`](../docs/active/experiments.md)。

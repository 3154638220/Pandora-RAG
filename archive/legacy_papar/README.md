# Legacy `papar/` snapshot

此目录保存原拼写错误目录 `papar/` 的完整代码快照，仅用于历史复现和版本差异追踪。

- 当前唯一实验代码入口是仓库根目录的 `stage1/`、`stage2/`、`stage3/`、`pretest/`、`qa_shared/` 和 `scripts/`。
- 快照中的代码、分析脚本和 Stop-RAG 工程可能与当前实现不一致。
- 新实验不得从本目录导入模块或写入结果。
